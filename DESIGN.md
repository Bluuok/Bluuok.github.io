# Bluuok / Digital Atelier

## 当前方向

四页个人数字创作工作室。冷白 `#fafbfc`、深灰 `#24262b`，蓝紫粉只出现在局部光线、主标题和概念装置中。现有粒子视觉保留；不复刻参考站的品牌或素材，不让装饰替代项目真实界面。

| 页面 | 主要艺术语言 | 内容责任 |
| --- | --- | --- |
| 首页 | 光尘 → 风与织物 | 作者介绍、两个核心项目、实验入口、关于与联系 |
| Clawtide | 刚性的三维陶瓷潮汐 | Web / IM / 定时入口、真实 UI、工程取舍与边界 |
| ThreadCove | 雾蓝纸页与关联细线 | 个人研究工作方式、真实 UI、上下文与事件流 |
| Playground | 安静的作品画廊 | 真实实验目录，主动开启 First Spark |

## 构图与交互

首页保持居中标题和现有粒子。织物是第二段，透明无框的场景与旁侧短文案共同构图；用户可直接跳到精选作品。织物局部形变用真实网格、指针射线命中与 UV 坐标实现，文字永远是稳定可选的 DOM。

Clawtide 左文右物，陶瓷有真实厚度和倒角，不使用一块 SVG 仪表盘代替三维作品。入口切换产生一次短光点，不持续播放无意义的管道动画。入口差异保持真实：租约不是用户认证，也不代表跨机器分布式锁。

ThreadCove 使用独立编辑页头、倾斜纸页和有限视差，HTML 内容与 SVG 线索共同表达关系。概念研究台不能暗示产品已实现自动知识图谱或自动事实核验。

项目真实界面在主视觉后尽快出现。截图保持产品原生配色；测试数据与真实线上数据分开说明，概念图必须有相应标签。

## First Spark

旧双手素材、实际指尖锚点和滚动叙事保留在 `/playground/#first-spark` 的页内展开状态，不再占首页，也不新增第五个路由。默认只显示轻量海报；点击后加载原手图、时间轴和场景。关闭后清理动画、钉定和循环，恢复入口焦点。

## 降级与维护

离屏/后台停止非必要帧；系统减少动态效果优先。无 JS / GL 失败时保留静态作品、导航与正文，不让动画成为内容访问门槛。移动设备保留纵向滚动与缩放，装饰不接管全页手势。

详细实现、素材来源、逐状态验收和未验证边界见 `docs/FOUR-PAGE-IMPLEMENTATION.md`。旧 `VERIFICATION.md` 是之前纸手/光尘更新的历史记录，不能直接作为本轮变更证据。
