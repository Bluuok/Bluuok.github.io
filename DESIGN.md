# Bluuok / First Spark

## 方向

以桌面浏览器为重点的明亮、极简个人作品集。参考 Antigravity 的留白与局部虹彩方向，不复用其品牌、文案或图片。冷白 #fafbfc、深灰 #24262b；蓝紫粉只出现在标题、光线、概念封面中。无外部字体请求。

全屏序章 → 双列宽幅作品 → 安静的个人介绍 → 直接联系。正文保持普通滚动。

## 六个关键状态

| 序章进度 | 画面 |
| --- | --- |
| 0–12% | 名字、定位、作品入口始终可见，双手只在边缘出现 |
| 12–40% | 标题让位，双手从两侧接近，虹彩反射稍增强 |
| 40–65% | 镜头围绕接触点缓慢放大，手部接近结束 |
| 65–75% | 两个指尖锚点重合，出现局部柔光与单圈虹彩 |
| 75–90% | 手部逐渐消隐，曝光过渡到正文同色的冷白 |
| 90–100% | 新标题浮现，随后解除固定，进入项目区 |

桌面额外滚动行程 2.2 个视口，手机 1.65 个；scrub 0.65 平滑进度。使用一条 GSAP 时间轴，不反复触发闪光事件，不缩放被 pin 的舞台。倒滚连续还原。

## 占位与最终素材边界

当前手部已更新为内置 ImageGen 绘制的漫画褶皱纸图层：细墨线、交叉排线、纸张折痕与浅冷色阴影。两只手姿势不同，从左上、右下沿对角线接近，按真实指尖像素坐标注册。项目封面仍是 CSS 概念占位，不是产品截图；功能、履历、项目成绩与外链均不编造。

手部先生成同一构图母图，再提取左右手。生成工具的第一次透明提取产物不含 alpha，最终采用纯色底编辑与确定性透明提取，白底检查边缘。每手原图 1536×1024，WebP 用于网页，PNG 同步保留。左上手指尖为 (1402,834)，右下手为 (116,99)。位置、大小、角度、接触点、分镜时序集中在 `src/features/intro/config.ts`；通过图层注册保持缩放和旋转时指尖不漂移。虹彩白化由代码完成。

背景为独立 Canvas 光尘模块，分两段：前段鼠标靠近时组成可替换的星芒或字符，移开后散开；后段跟随序章进度汇聚到双手实际指尖中点。细碎光尘使用统一的冷色光晕、亮芯和柔光层，保持正文可读。图案、位置、密度、亮度和切换时机独立配置；`features.particles` 可关闭。离屏/后台暂停，减少动态效果时静态显示，不拦截点击。

2026-09-12 实际浏览 Antigravity 官网并检查鼠标移动前后画面，参考其成片流动、密度变化与空间感。本站进一步按用户偏好加强发光和汇聚，不复刻其图标或品牌素材；开场纸手构图保持不变。

项目素材：优先真实截图，每张约 1600×1000，保留项目图与概念图的区别。放在 public/images/，填写 src/data/projects.ts 的 image 和 imageAlt。

## 可访问性与扩展

减少动态效果时不钉定、不镜头缩放、不白化；无 JavaScript 时仍显示静态开场和完整正文。文字可选中复制。固定头部与跳过链接始终可用，隐藏的开场按钮从键盘焦点移除。手机号、履历、域名尚未提供，不添加虚构内容。

参考：https://antigravity.google/ 和 https://gsap.com/docs/v3/Plugins/ScrollTrigger/。
