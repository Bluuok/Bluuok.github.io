# 验证记录 — 2026-09-12（两段发光光尘）

## 当前版本

- 类型检查：27 个文件，0 errors、0 warnings、0 hints；静态构建的 3 个页面成功。
- Edge / Playwright：主站 43/43、独立组件与触控 14/14，共 57 项浏览器检查通过，无运行时错误。
- 功能开关构建：4/4 通过，覆盖独立关闭粒子、关闭开场、空区块、项目页返回首页及恢复默认配置。
- 默认 1440×1000 舞台为 5200 个光尘；390×844 为 1600 个，随面积调整，默认上限 6400。
- 本机无头 Edge 在星芒状态采样 120 帧，间隔中位数 18.1ms、P95 18.4ms。这个数字只反映本次环境，不代表所有设备或浏览器的帧率。

实际检查鼠标接近组成八芒星、离开后散开，向下滚动汇聚到双指尖中点、淡出、倒滚恢复。读取 Canvas 像素验证形状区域光量增加与光核位置，并查看实际截图，确认没有彩色圆圈边缘或矩形压缩轮廓。

独立实例验证替换字符 `B`、三字符 `WWW`、修改形状中心、外部输入汇聚焦点、局部阶段配置、短十六进制及 RGB 配色、卸载/重新挂载、淡出停止和逆向恢复。外部焦点设为 `(0.7,0.4)` 时，实测光量重心为约 `(0.699,0.399)`。触屏轻触可触发图案，稍后自动散开。

纸手对角构图、指尖重合、窗口缩放、640px 断点、跳过开场、项目页跳转与返回、直接哈希、减少动态效果、无 JavaScript 与手图加载失败回退均通过回归。纸手图片和项目内容保持原样。

源码独立审查由 Astra low 代理完成。发现的 CSS 颜色透明渐变与多字符画布裁切问题已修复并复核；最终绘制优化也已复核。真实截图和 JSON 结果位于 `docs/verification-luminous/`。该目录保留本机验证脚本；配置检查脚本会临时修改 `features.ts`，先停止开发服务，脚本在 finally 中恢复原配置并重建。

参考站于本日通过浏览器实际访问 [Google Antigravity](https://antigravity.google/)，检查了首屏和鼠标移动后的粒子分布。本站使用自己的 Canvas 模块实现，未复制其品牌素材。

修改入口见 `docs/CUSTOMIZATION.md`；核心参数在 `src/features/particles/config.ts`。

---

以下保留前轮纸手更新的历史验证记录。

## 前轮纸手结果

- Node 24.19.0、Astro 7.3.2、GSAP 3.15.0，无新增运行库。
- 类型检查：24 个文件，0 errors、0 warnings、0 hints。
- 静态构建：首页、Clawtide 与 ThreadCove 两个项目页全部成功。
- Edge / Playwright 浏览器验收：35/35 通过，主页面无运行时异常或 HTTP 资源错误。
- 独立关闭粒子：实际改为 particles:false 后构建，粒子元素消失，纸手保留；随后恢复默认配置并重新构建。
- 左右手 WebP 均有真实 alpha；PNG 母版保留。已在白底检查轮廓与指缝，手部两张 WebP 合计 378506 字节。

## 浏览器验收内容

桌面 1440×1000：对角起始位置、图像解码、五个滚动位置钉定、接触点误差小于 2px、倒滚恢复、快速滚轮往返、跳过、项目详情返回、直接 /#projects 哈希、无横向溢出。

实际读取 Canvas 在鼠标附近区域的像素 alpha 总量，停留后大于移动前的 1.2 倍，确认聚拢不是只有事件回调。验证鼠标移出后释放、开场离屏时帧循环暂停。

手机 390×844：基本布局与接触点；1920→1280 视口变更后仍能对齐；640px 边界处 CSS/JS 都采用同一桌面布局且指尖对齐。

减少动态效果：无钉定、无长滚动、粒子静态；运行时修改偏好能清理钉定。禁用 JavaScript 时正文入口仍可用。拦截两张手图加载时自动撤销钉定并允许进入项目区。

截图与 JSON 结果：docs/verification-paper/。实际查看桌面首屏、鼠标汇聚、触碰、柔和白化与手机首屏。

## 独立审查

按全局约定，粒子模块由 Sol medium 实现，Astra low 原生代理独立只读审查。发现手部移动断点配置与 CSS 脱节，已统一来源并复核；主代理浏览器检查发现自定义元素 hidden=false 被序列化为隐藏属性，已修复并通过回归。

未宣称使用未经核实的 Gemini 或 Claude 模型。

## 范围

本轮更新手部视觉、对角构图和鼠标粒子。项目内容仍是待填框架；没有修改 Clawtide、ThreadCove 的业务代码，没有发布到外网。

手部是分层图片，不是实时三维关节动画。未量化低端设备性能，未进行所有浏览器的兼容测试。原版框架的清空区块、关闭开场构建检查已在前轮完成。

修改说明：docs/CUSTOMIZATION.md。内置 ImageGen 的完整提示词与提取过程：docs/artwork/PROMPTS.md。
